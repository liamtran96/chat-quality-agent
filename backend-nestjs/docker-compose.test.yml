version: '3.8'

services:
  postgres-test:
    image: postgres:16
    environment:
      POSTGRES_DB: cqa_test
      POSTGRES_USER: cqa
      POSTGRES_PASSWORD: cqa_password
    ports:
      - '5433:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U cqa -d cqa_test']
      interval: 5s
      timeout: 5s
      retries: 5
