version: '3.8'

services:
  postgres-test:
    image: postgres:16
    environment:
      POSTGRES_DB: cqa_test
      POSTGRES_USER: cqa_test
      POSTGRES_PASSWORD: cqa_test_pass
    ports:
      - '5433:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U cqa_test']
      interval: 5s
      timeout: 5s
      retries: 5
