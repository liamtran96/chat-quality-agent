services:
  postgres-test:
    image: postgres:16
    environment:
      POSTGRES_DB: cqa_test
      POSTGRES_USER: cqa_test
      POSTGRES_PASSWORD: cqa_test_pass
    ports:
      - '5433:5432'
    tmpfs:
      - /var/lib/postgresql/data
